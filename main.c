#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "libft.h"

int main () {
   // // char **t;
   // printf("%s", ft_split("hello,hi,,,dear,,", ','));
   // char * invalidReadCheck =  0;
   printf("%s", ft_split("0invali0dReadCheck", '0'));

   return(0);
}